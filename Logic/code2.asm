CODE SEGMENT
ASSUME CS:CODE, DS:CODE

ORG 1000H
MOV AX, CS
MOV DS, AX

MOV AX, 70H
MOV BX, 40H

AND AX, BX; AX = AX AND BX
OR AX, BX; AX = AX OR BX
XOR AX, BX; AX = AX XOR BX
NOT AX; AX = AX'

CODE ENDS
END